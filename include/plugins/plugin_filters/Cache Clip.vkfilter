name = "Cache Clip"
category = "Utility"
code = """
# Cache clip frames in memory for faster access

from vstools import vs, core

# Make recently used frames cached
clip = core.std.Cache(clip, make_linear=True)"""
description = "Caches clip frames in memory for performance optimization"

[metadata]
createdAt = "2026-01-31T00:00:00.000Z"
